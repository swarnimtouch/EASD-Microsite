<div class="medihub-page-heading">
    <div>
        <div class="medihub-eyebrow">{{ $eyebrow }}</div>
        <h1>{{ $heading }}</h1>
        <p>{{ $description }}</p>
    </div>
    <a href="{{ $backUrl }}" class="mh-btn-secondary"><i class="bi bi-arrow-left me-2"></i>{{ $backLabel }}</a>
</div>
